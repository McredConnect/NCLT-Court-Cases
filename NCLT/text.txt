after installing dependencies run this playwright install run this on Terminal

API Endpoints
/scrape: gives combined result
/stream-scrape: Similar POST input, but streams after every process

1.Install requirements.txt 
2.cmd: playwright install

uvicorn app:app --host 127.0.0.1 --port 8000 --workers 1  
#set workers only 1 it will take multiple api calls

Endpoints are:

POST /scrape — send company name, courts, years (2021-2025), and case status; get PDFs to view and download.
{
  "company_name": "reliance industries",
  "benches": [
    "Delhi", "Mumbai"
  ],
  "years": [
    2024, 2022, 2025
  ],
  "statuses": [
    "Pending",
    "Disposed"
  ]
}